# openclaw-agent-benchmark
Two-agent OpenClaw compliance pipeline benchmarked at 1/5/10 concurrent sessions — Qwen2.5-7B on NVIDIA T4 via Ollama and vLLM. Finds the concurrency crossover point where sequential queuing collapses.
